# 📊 دليل نظام الحسابات والتقارير - محطة الوقود

## 📚 المحتويات
1. [نظرة عامة](#نظرة-عامة)
2. [هيكل النظام](#هيكل-النظام)
3. [الجداول والنماذج](#الجداول-والنماذج)
4. [التقارير المتاحة](#التقارير-المتاحة)
5. [شرح الحسابات](#شرح-الحسابات)
6. [أمثلة الاستخدام](#أمثلة-الاستخدام)

---

## 🎯 نظرة عامة

نظام الحسابات والتقارير هو نظام متكامل لإدارة المعاملات المالية لمحطة الوقود، يشمل:

- **📥 الإيرادات**: تتبع مبيعات الوقود (نقدي وآجل) ودفعات العملاء
- **📤 المصروفات**: تسجيل كافة المصروفات المتنوعة
- **👥 حسابات العملاء**: متابعة الديون والمستحقات
- **📊 التقارير**: تقارير تفصيلية وملخصة

---

## 🏗️ هيكل النظام

### الجداول الرئيسية:

#### 1. `treasury_transactions` (معاملات الخزنة)
جدول موحد لكل الإيرادات والمصروفات

```php
- id
- type              // 'income' أو 'expense'
- category          // التصنيف (رأس المال، دفعة عميل، مشتريات، إلخ)
- amount            // المبلغ
- transaction_date  // تاريخ العملية
- description       // الوصف
- user_id           // المستخدم المسؤول
```

#### 2. `transactions` (معاملات البيع)
معاملات بيع الوقود اليومية

```php
- id
- shift_id          // الشيفت
- nozzle_id         // المسدس المستخدم
- client_id         // العميل (null = نقدي، موجود = آجل)
- total_amount      // المبلغ الإجمالي
- cash_liters       // لترات نقدي
- credit_liters     // لترات آجل
```

#### 3. `clients` (العملاء)
بيانات العملاء

```php
- id
- name              // اسم العميل
- pump_id           // المضخة المخصصة
- is_active         // نشط أم لا
```

---

## 📊 التقارير المتاحة

### 1. 📈 التقرير المالي الشامل `/reports`

**الوظيفة**: عرض شامل لكل الحركات المالية

**المميزات**:
- فلترة حسب الفترة الزمنية (من - إلى)
- فلترة حسب نوع الدفع:
  - **الكل**: جميع المعاملات (نقدي + آجل + مصروفات)
  - **نقدي**: المبيعات النقدية + دفعات العملاء + المصروفات
  - **آجل**: فقط المبيعات الآجلة

**البيانات المعروضة**:
```
✅ رأس المال
✅ إجمالي الإيرادات
✅ إجمالي المصروفات
✅ صافي الربح/الخسارة
✅ تفاصيل الإيرادات حسب نوع الوقود
✅ تفاصيل المصروفات حسب الفئة
✅ سجل المعاملات اليومي مع الرصيد التراكمي
```

### 2. 💰 تقرير الإيرادات `/reports/revenues`

**الوظيفة**: تقرير تفصيلي بكل عمليات البيع

**المميزات**:
- فلترة حسب الفترة الزمنية
- عرض تفاصيل كل عملية بيع
- تقسيم بين نقدي وآجل

**البيانات المعروضة**:
```
✅ إجمالي الإيرادات
✅ الإيرادات النقدية
✅ الإيرادات الآجلة
✅ تفاصيل كل عملية:
   - التاريخ والوقت
   - نوع الوقود
   - العميل (إن وجد)
   - الشيفت والموظف
   - المبلغ
```

### 3. 💸 تقرير المصروفات `/reports/expenses`

**الوظيفة**: تقرير تفصيلي بكل المصروفات

**المميزات**:
- فلترة حسب الفترة الزمنية
- تصنيف المصروفات حسب الفئة
- رسم بياني للنسب المئوية

**البيانات المعروضة**:
```
✅ إجمالي المصروفات
✅ عدد المصروفات
✅ تقسيم حسب الفئة مع النسب المئوية
✅ تفاصيل كل مصروف:
   - التاريخ
   - الفئة
   - البيان
   - المسؤول
   - المبلغ
```

**الفئات المتاحة**:
- مشتريات وقود
- صيانة
- رواتب
- مرافق (كهرباء، مياه، إلخ)
- أخرى

### 4. 👥 تقرير حسابات العملاء `/reports/clients`

**الوظيفة**: متابعة الديون والمستحقات على العملاء

**البيانات المعروضة**:
```
✅ إجمالي الديون
✅ عدد العملاء
✅ متوسط الدين لكل عميل
✅ تفاصيل كل عميل:
   - الاسم
   - نوع الوقود
   - إجمالي المشتريات
   - المدفوع
   - الرصيد المستحق
```

**ملاحظة**: يعرض فقط العملاء الذين عليهم ديون (رصيد > 0)

---

## 💡 شرح الحسابات

### حساب الإيرادات

```
إجمالي الإيرادات = 
    (مبيعات نقدية) + 
    (مبيعات آجلة) + 
    (دفعات العملاء)
```

**مثال**:
- مبيعات نقدية: 10,000 ج.م
- مبيعات آجلة: 5,000 ج.م
- دفعات من عملاء: 2,000 ج.م
- **الإجمالي: 17,000 ج.م**

### حساب المصروفات

```
إجمالي المصروفات = 
    مجموع كل المعاملات من نوع 'expense'
    في جدول treasury_transactions
```

**مثال**:
- مشتريات وقود: 8,000 ج.م
- صيانة: 500 ج.م
- رواتب: 2,000 ج.م
- **الإجمالي: 10,500 ج.م**

### حساب صافي الربح

```
صافي الربح = إجمالي الإيرادات - إجمالي المصروفات
```

**مثال**:
- إيرادات: 17,000 ج.م
- مصروفات: 10,500 ج.م
- **صافي الربح: 6,500 ج.م**

### حساب ديون العميل

```php
// لكل عميل:
إجمالي المشتريات = مجموع مبيعات الوقود الآجلة للعميل
إجمالي المدفوعات = مجموع دفعات العميل في الخزنة
الرصيد المستحق = إجمالي المشتريات - إجمالي المدفوعات
```

**مثال (عميل أحمد)**:
- مشتريات آجلة: 10,000 ج.م
- دفعات مسجلة: 7,000 ج.م
- **الرصيد المستحق: 3,000 ج.م**

---

## 🔄 دورة العمل الكاملة

### 1. إضافة رأس المال (مرة واحدة عند البداية)
```
الخزنة → إضافة إيراد
النوع: income
الفئة: رأس المال
المبلغ: 100,000 ج.م
```

### 2. تسجيل مبيعات (يومياً)

**بيع نقدي**:
```
المعاملات → إضافة معاملة
العميل: فارغ (null)
المبلغ: يُحسب تلقائياً
```

**بيع آجل**:
```
المعاملات → إضافة معاملة
العميل: اختيار العميل
المبلغ: يُسجل كدين على العميل
```

### 3. تسجيل دفعة عميل
```
العملاء → [اسم العميل] → إضافة دفعة
المبلغ: 5,000 ج.م
→ يُسجل تلقائياً في الخزنة كإيراد
→ يُخصم من رصيد العميل
```

### 4. تسجيل مصروف
```
المصروفات → إضافة مصروف
الفئة: مشتريات / صيانة / رواتب / إلخ
المبلغ: 2,000 ج.م
→ يُسجل تلقائياً في الخزنة كمصروف
```

### 5. مراجعة التقارير
```
التقارير → كشف الحساب المالي
→ اختيار الفترة
→ اختيار نوع المعاملة (الكل/نقدي/آجل)
→ عرض التقرير الشامل
```

---

## 📍 مسارات API

```php
// التقرير الرئيسي
GET /reports
    ?start_date=2024-01-01
    &end_date=2024-01-31
    &payment_type=all|cash|credit

// تقرير الإيرادات
GET /reports/revenues
    ?start_date=2024-01-01
    &end_date=2024-01-31

// تقرير المصروفات
GET /reports/expenses
    ?start_date=2024-01-01
    &end_date=2024-01-31

// تقرير حسابات العملاء
GET /reports/clients
```

---

## 🎨 مميزات واجهة المستخدم

### 📊 البطاقات الملونة
- **أزرق**: رأس المال
- **أخضر**: الإيرادات
- **أحمر**: المصروفات
- **رمادي**: صافي الربح

### 📈 الرسوم البيانية
- نسب المصروفات حسب الفئة
- تطور الرصيد التراكمي

### 🖨️ الطباعة
جميع التقارير قابلة للطباعة بتنسيق احترافي

### 🔍 الفلترة
- حسب الفترة الزمنية
- حسب نوع الدفع
- حسب العميل

---

## ⚠️ ملاحظات هامة

### 1. الفرق بين النقدي والآجل
```
النقدي: 
- لا يوجد client_id في المعاملة
- يدخل مباشرة في الخزنة

الآجل:
- يوجد client_id في المعاملة
- يُسجل كدين على العميل
- يدخل الخزنة عند الدفع فقط
```

### 2. تسجيل دفعات العملاء
```
عند إضافة دفعة عميل:
1. تُسجل في treasury_transactions كـ income
2. الفئة: "دفعة عميل"
3. الوصف: يحتوي على اسم العميل
4. تُخصم من رصيد العميل
```

### 3. تصنيف المصروفات
```
كل المصروفات تُسجل في treasury_transactions
النوع: expense
الفئة: تُحدد من قائمة منسدلة
```

### 4. الرصيد التراكمي
```
يبدأ من 0
يزيد مع كل إيراد
يقل مع كل مصروف
يعرض في التقرير الرئيسي
```

---

## 🔧 كود مرجعي

### استعلام الإيرادات
```php
$revenues = Transaction::with(['nozzle.pump.tank.fuel', 'shift', 'client'])
    ->whereDate('created_at', '>=', $startDate)
    ->whereDate('created_at', '<=', $endDate)
    ->when($paymentType == 'credit', function ($q) {
        return $q->whereNotNull('client_id');
    })
    ->when($paymentType == 'cash', function ($q) {
        return $q->whereNull('client_id');
    })
    ->get();
```

### استعلام المصروفات
```php
$expenses = TreasuryTransaction::where('type', 'expense')
    ->whereDate('transaction_date', '>=', $startDate)
    ->whereDate('transaction_date', '<=', $endDate)
    ->get();
```

### حساب ديون العميل
```php
$totalTransactions = $client->transactions->sum('total_amount');
$totalPayments = TreasuryTransaction::where('type', 'income')
    ->where('category', 'دفعة عميل')
    ->where('description', 'like', '%' . $client->name . '%')
    ->sum('amount');
$balance = $totalTransactions - $totalPayments;
```

---

## 📞 الدعم والمساعدة

في حالة وجود أي استفسارات أو مشاكل:
1. راجع هذا الدليل أولاً
2. تحقق من البيانات المدخلة
3. تأكد من صحة التواريخ والفترات
4. راجع سجل المعاملات في الخزنة

---

**آخر تحديث**: {{ now()->format('Y-m-d') }}
**الإصدار**: 2.0
